diff --git a/modules/juce_core/native/juce_android_Threads.cpp b/modules/juce_core/native/juce_android_Threads.cpp
index f9ba13b0ac..49a1689226 100644
--- a/modules/juce_core/native/juce_android_Threads.cpp
+++ b/modules/juce_core/native/juce_android_Threads.cpp
@@ -270,7 +270,8 @@ private:
 
 //==============================================================================
 #if JUCE_MODULE_AVAILABLE_juce_events && JUCE_ANDROID
-void juce_juceEventsAndroidStartApp();
+extern "C" void juce_juceEventsAndroidStartApp();
+void juce_ensureEventsLoopStarted();
 #endif
 
 void Thread::initialiseJUCE (void* jniEnv, void* context)
@@ -304,7 +305,8 @@ void Thread::initialiseJUCE (void* jniEnv, void* context)
         JuceActivityWatcher::getInstance();
 
        #if JUCE_MODULE_AVAILABLE_juce_events && JUCE_ANDROID
-        juce_juceEventsAndroidStartApp();
+        //juce_juceEventsAndroidStartApp();
+        juce_ensureEventsLoopStarted();
        #endif
     }
 }
diff --git a/modules/juce_events/native/juce_android_Messaging.cpp b/modules/juce_events/native/juce_android_Messaging.cpp
index 20f5f50ced..0ba729102e 100644
--- a/modules/juce_events/native/juce_android_Messaging.cpp
+++ b/modules/juce_events/native/juce_android_Messaging.cpp
@@ -288,7 +288,7 @@ private:
 //==============================================================================
 File juce_getExecutableFile();
 
-void juce_juceEventsAndroidStartApp()
+extern "C" void juce_juceEventsAndroidStartApp()
 {
     auto dllPath = juce_getExecutableFile().getFullPathName();
     auto addr = reinterpret_cast<juce::JUCEApplicationBase*(*)()> (DynamicLibrary (dllPath)
@@ -298,4 +298,11 @@ void juce_juceEventsAndroidStartApp()
         JuceAppLifecycle::getInstance (addr);
 }
 
+void juce_ensureEventsLoopStarted()
+{
+    // This ensures that event loop exists and it's on Android's main thread.
+    // Note that to ensure that it is the main thread, it must be indeed invoked as such e.g. at androidx.startup.
+    MessageManager::getInstance();
+}
+
 } // namespace juce
